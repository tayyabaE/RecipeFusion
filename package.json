{
  "dependencies": {
    "apexcharts": "^4.5.0",
    "concurrently": "^9.1.2",
    "express": "^5.1.0",
    "react-apexcharts": "^1.7.0"
  },
  "scripts": {
    "both": "concurrently \"cd client && npm run dev -- --host\" \"cd server && npm run dev -- --host\""
  }
}
